<div id="save-throw" class="dialogue-content" onsubmit="event.preventDefault();">
{{log effectId}}
	<div class="form-group vertical">
	
		<h1 class="form-title">
		{{#if effectName}}
			{{localize 'DND4E.SaveVsPrompt'}} "<strong>{{effectName}}</strong>"
		{{else}}
			{{localize 'DND4E.SavePrompt'}} 
		{{/if}}
		</h1>
		
		{{#unless effectSave}}
		{{#if savableEffects.length}}
			<div class="form-group">
				<label>{{ localize "DND4E.SaveAgainst" }}</label>
				<select name="saveAgainst" class="refreshes">
					{{selectOptions savableEffects selected=effectId localise=true valueAttr="id" labelAttr="name"}}
				</select>
			</div>
		{{/if}}
		{{/unless}}
		
		<div class="form-group">
			<label>{{localize 'DND4E.SaveDCSet'}}:</label>
			<input type="text" name="dc" value="{{#if saveDC}}{{saveDC}}{{else}}10{{/if}}" placeholder="10" />
		</div>

		<div class="form-group">
			<label>{{localize 'DND4E.SaveSituational'}}?</label>
			<input type="text" name="save" value="" placeholder="" />
		</div>

		<div class="form-group">
			<label>{{ localize "DND4E.RollMode" }}</label>
			<select name="rollMode">
				{{selectOptions rollModes selected=rollMode localize=true}}
			</select>
		</div>
		
	</div>
	
</div>
